{
  "name": "FanCodes2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"if [[ $(node --version) == v8.* ]]; then echo 'PASS'; else echo 'FAIL\nnode major version must be grether then 8, currently $(node --version)'; fi\" | bash ",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv": "^1.1.1",
    "node-fetch": "^1.7.3"
  }
}
